<%-
  ncpus = num_cores.blank? ? 2 : num_cores.to_i
  mem = ncpus * 4
-%>
---
batch_connect:
  template: "basic"
script:
  job_name: "rstudio_ood_test"
  native:
    - "-l"
    - "select=1:ncpus=<%= ncpus %>:mem=<%= mem %>gb"
  email: "<%= ENV['USER'] %>@unsw.edu.au" 
